2 users

test: login
testIndex: search
testForm: add/edit form
testConfig: contains database connection and a few other different things (such as login handling if someone tries to access site w/o logging in)
testFunction: contains random useful functions such as a get() for handling empty Pass-by-URL, and testing to see if someone has admin status
